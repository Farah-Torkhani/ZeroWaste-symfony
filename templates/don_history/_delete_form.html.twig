<form method="post" action="{{ path('app_don_history_delete', {'id': don_history.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ don_history.id) }}">
    <button class="btn">Delete</button>
</form>
